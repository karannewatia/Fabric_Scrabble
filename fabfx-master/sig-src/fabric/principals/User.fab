package fabric.principals;

public class User extends ExternalPrincipal {

	public User() {
		super("GUI User");
	}

	public boolean{this;p;this<-} delegatesTo{this->}(principal {this->} p) {
	  return super.delegatesTo(p) || p == FabricWorker.getWorker();
	}

}
