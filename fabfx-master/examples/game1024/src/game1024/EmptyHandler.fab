package game1024;

import fabfx.event.EventHandler;
import fabfx.event.Event;

public class EmptyHandler [label M] extends EventHandler[M]
		where M <= {User->}, {User<-} <= M {
	
	public void handle{M} (Event[M]{M} e) {
		
	}

}