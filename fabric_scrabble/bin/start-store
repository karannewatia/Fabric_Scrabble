#!/bin/bash

APP_HOME="$(dirname $0)/.."
FABRIC_HOME="${APP_HOME}/../../../../Downloads/fabric"
PATH="${FABRIC_HOME}/bin:${PATH}"

export FABRIC_HOME="${APP_HOME}"

fab-store --jvm-cp "${FABRIC_HOME}/lib/fabric-collections.jar:${APP_HOME}/classes" --app-home "${FABRIC_HOME}" --name store "$@"
