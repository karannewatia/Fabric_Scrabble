import fabfx.application.Application;
import fabfx.stage.Stage;
import fabric.principals.User;

public class GuiMain {

	public static void main {User->;User<-}
			(principal{p<-} p, String[] args):{User->;User<-}
			throws Exception {
		atomic {
			new Gui().launchApp();
		}
	}

}
